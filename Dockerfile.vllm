FROM vllm/vllm-openai:latest

# Set working directory
WORKDIR /workspace

# Expose the default vLLM port
EXPOSE 8000

# Default command (will be overridden by podman run)
CMD ["vllm", "serve", "--help"]
